# Runtime dependencies for the Fraud Sentinel pipeline.
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
# (the --extra-index-url is needed for the CUDA build of torch; omit it and
# torch will install as CPU-only, which the pipeline also supports, just slower)

pandas==3.0.6
numpy==2.5.3
torch==2.5.1
transformers==5.17.0
accelerate==1.15.0
peft==0.21.0
bitsandbytes==0.50.2
datasets==5.0.1
huggingface_hub==1.32.0
sentencepiece==0.2.2
protobuf==7.36.2

# Notebook execution (the hackathon deliverable is a Jupyter notebook)
jupyter==1.1.1
ipykernel==7.3.0
nbformat==5.11.1
